<%= form_with(model: island, local: true) do |form| %>
  <% if island.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(island.errors.count, "error") %> prohibited this island from being saved:</h2>

      <ul>
      <% island.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :island_name %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :island_user_id %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :island_description %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_field :text, id: :island_text %>
  </div>

  <div class="field">
    <%= form.label :icon %>
    <%= form.text_field :icon, id: :island_icon %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
